import { StyleSheet } from "react-native";

// No tokens needed — pure positioning, matching the web wrapper's inline styles.
export const styles = StyleSheet.create({
  toast: { position: "absolute", left: 16, right: 16, alignItems: "center", zIndex: 50 },
});
